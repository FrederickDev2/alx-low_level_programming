#include "main.h"
/**
 * _abs - computes absolute value of an interger
 * @n: is an integer
 *
 * Return: nothing
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else if (n > 0)
	{
		return (n);
	}
	else
	{
		return (n);
	}
}
